
mixin breadcrumbs(items, cls)
    ul.breadcrumb(class=typeof cls == 'undefined' ? undefined : cls)
        each item, index in items
            if index + 1 != items.length
                li.breadcrumb__item
                    a(href="javascript:;")=item
                li.breadcrumb__item.breadcrumb__item_separator >
            else
                .breadcrumb__item.breadcrumb__item_active=item
